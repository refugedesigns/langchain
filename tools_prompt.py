'''
You have access to the following tools:

run_query: runs a sqlite query and returns the result. Accepts an argument of a sql query as string.

To use a tool always repond with the following format:

{
    "name": <tool_name>
    "argument": <tool_argument>
}
'''
